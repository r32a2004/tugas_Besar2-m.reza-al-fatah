<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".util.MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Uas_muhammadRezaAlFatah">

        <activity
            android:name=".subject_crud.subject_list_show.SubjectListActivity"
            android:label="@string/title_activity_subjects"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".taken_subject_crud.taken_subject_show.StudentTakenSubjectActivity"
            android:label="@string/student_and_taken_subject"
            android:theme="@style/Theme.AppCompat.Light"
            android:exported="false" />

        <activity
            android:name=".taken_subject_crud.subject_assign.SubjectAssignActivity"
            android:label="@string/subject_assign"
            android:theme="@style/Theme.AppCompat.Light.Dialog"
            android:exported="false" />

        <activity
            android:name=".student_crud.student_list_show.StudentListActivity"
            android:label="@string/student_list"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>